# Farm2Home LK

A complete frontend-only React marketplace for Sri Lankan farmers and customers, with persistent mock data.

```sh
cd frontend
npm install
npm run dev
```

See [the frontend README](frontend/README.md) for demo credentials, workflows, architecture, routes, tests and integration notes.
