VITE_API_URL=http://localhost:5000/api
# Optional advisor API mode requires a real JWT from marketplaceApi.auth.login.
# The rest of the existing frontend continues to use mock Context state.
VITE_PRICE_ADVISOR_MODE=mock
