import { Link,useNavigate } from 'react-router-dom';
import { Heart,Plus,MapPin,ShoppingBasket,ArrowRight } from 'lucide-react';
import { useMarket,useCart,useWishlist,useAuth } from '../../context/AppContext';
import { Img,PriceDisplay,RatingStars,StockBadge } from '../common/UI';
export default function ProductCard({product:p}){const {farmers}=useMarket();const {addToCart}=useCart();const {wishlist,toggleWish}=useWishlist();const {user}=useAuth();const navigate=useNavigate();const farmer=farmers.find(f=>f.id===p.farmerId);return <article className="product-card"><div className="product-image"><Link to={'/products/'+p.id}><Img src={p.images[0]} alt={p.name}/></Link><span className={'product-tag '+(p.method==='Organic'?'organic':'')}>{p.availability==='Upcoming Harvest'?'Upcoming harvest':p.method==='Organic'?'Organic':p.quality}</span><button className={'wish-btn '+(wishlist.includes(p.id)?'selected':'')} aria-label={(wishlist.includes(p.id)?'Remove from':'Add to')+' wishlist: '+p.name} onClick={()=>user?.role==='farmer'?navigate('/unauthorized'):toggleWish(p.id)}><Heart size={18} fill={wishlist.includes(p.id)?'currentColor':'none'}/></button></div><div className="product-body"><div className="between"><span className="product-category">{p.category}</span><RatingStars rating={p.rating} count={p.reviewCount}/></div><Link to={'/products/'+p.id}><h3>{p.name}</h3></Link><Link className="farm-label" to={'/farmers/'+p.farmerId}>{farmer?.farm||'Local family farm'}</Link><span className="location"><MapPin size={12}/>{p.district}<span>•</span>{p.quantity} {p.unit} available</span><div className="product-bottom"><PriceDisplay price={p.price} unit={p.unit}/><button className="add-btn" disabled={p.quantity===0||p.availability==='Sold Out'} aria-label={'Add '+p.name+' to cart'} onClick={()=>addToCart(p)}>{p.availability==='Upcoming Harvest'?<ShoppingBasket size={18}/>:<Plus size={20}/>}</button></div>{['Sold Out','Low Stock','Upcoming Harvest'].includes(p.availability)&&<StockBadge status={p.availability}/>}</div></article>;}
export function ProductGrid({products}){return <div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>;}
export function CategoryCard({name,image,count=0}){return <Link className="category-card" to={'/products?category='+encodeURIComponent(name)}><div><Img src={image} alt={name}/></div><h3>{name}</h3><span>{count} products <ArrowRight size={13}/></span></Link>;}
