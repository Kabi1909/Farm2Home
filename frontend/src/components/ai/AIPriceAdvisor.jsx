import { useState } from 'react';
import { Sparkles,ArrowRight,RefreshCw } from 'lucide-react';
import { getPriceSuggestion } from '../../services/aiService';
import { money } from '../../utils/helpers';
import { Checkbox } from '../common/UI';
export default function AIPriceAdvisor({product,onApply,onManual}){const [result,setResult]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState(''),[simulateError,setSimulateError]=useState(false);async function suggest(){setBusy(true);setResult(null);setError('');try{setResult(await getPriceSuggestion({...product,month:new Date(product.harvestDate).getMonth()+1,simulateError}));}catch(err){setError(err.message);}finally{setBusy(false);}}return <section className="ai-advisor"><div className="ai-title"><span><Sparkles size={26}/></span><div><span className="eyebrow">A LITTLE GUIDANCE FOR YOUR HARVEST</span><h2>AI Smart Price Advisor</h2></div><span className="badge green">Mock AI</span></div><p>Find a thoughtful starting price for your produce. You’re always in control.</p><div className="ai-inputs"><span><small>Product</small><strong>{product.name||'Your produce'}</strong></span><span><small>Location</small><strong>{product.district}</strong></span><span><small>Quality</small><strong>{product.quality}</strong></span><span><small>Quantity</small><strong>{product.quantity} {product.unit}</strong></span></div>{!result&&!error&&<button type="button" className="btn" disabled={busy} onClick={suggest}>{busy?<><span className="spinner"/>Analyzing marketplace data…</>:<><Sparkles size={18}/>Get AI Price Suggestion</>}</button>}{result&&<div className="ai-result"><span className="eyebrow">RECOMMENDED PRICE</span><div className="ai-price">{money(result.recommendedPrice)}<small> / {product.unit}</small></div><p className="price-range">Suggested range: {money(result.minimumPrice)} – {money(result.maximumPrice)} / {product.unit}</p><span className="badge green">{result.confidence} confidence · simulated estimate</span><div className="actions"><button type="button" className="btn" onClick={()=>onApply(result.recommendedPrice)}>Use {money(result.recommendedPrice)} <ArrowRight size={16}/></button><button type="button" className="btn secondary" onClick={onManual}>Enter my own price</button><button type="button" aria-label="Refresh suggestion" className="icon-btn" onClick={suggest}><RefreshCw size={18}/></button></div></div>}{error&&<div className="ai-result"><h3 className="error-text">{error}</h3><p>You can continue by entering your own selling price.</p><div className="actions"><button type="button" className="btn" onClick={suggest}>Try Again</button><button type="button" className="btn secondary" onClick={onManual}>Enter Price Manually</button></div></div>}<p className="ai-disclaimer">AI price suggestions are estimates based on available marketplace data and may not reflect the exact current market price. You can always set your own selling price.</p><Checkbox label="Demo: simulate AI unavailable" checked={simulateError} onChange={e=>{setSimulateError(e.target.checked);setResult(null);setError('');}}/></section>;}
